[pytest]
testpaths = tests
pythonpath = .

# La rete e' spenta per default. Un test che la vuole davvero deve dirlo, e
# allora si vede nel sorgente e in `pytest -m network`.
markers =
    network: il test apre connessioni vere (escluso dai giri normali)

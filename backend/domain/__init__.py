"""
Matematica pura: zero I/O, zero rete, zero database.
# feat (Blocco 6): il posto dove il calcolo si puo' testare senza montare niente.

Qui dentro le funzioni prendono numeri e ritornano numeri. Nessun modulo di
questa cartella importa `core/`, `data/` o `api/`: se un calcolo ha bisogno di
un dato, glielo passa chi lo chiama. E' la stessa separazione che sul frontend
tiene `lib/rilevatore.js` senza stato.
"""

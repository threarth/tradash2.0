# tradash2.0 — dipendenze dell'applicazione.
# Solo cio' che serve per FARLA GIRARE. Gli strumenti di sviluppo stanno in
# requirements-dev.txt: chi installa per usare l'applicazione non deve
# ritrovarsi pytest addosso.
Flask==3.1.3

# Defeatbeta: la fonte unica dei dati di mercato. 58k download/mese, ultima
# release il 10/06/2026. Si porta dietro 14 dipendenze d'obbligo (fra cui duckdb,
# pandas, nltk, openai, matplotlib): pesa, e in cambio da' l'URL delle tabelle e
# soprattutto l'invalidazione della cache quando Defeatbeta pubblica dati nuovi.
defeatbeta-api==0.0.60

# Il client ufficiale di Anthropic: e' il solo modo in cui questo sistema parla
# con un modello, e passa tutto da `core/llm.py`. 1.2.0 pubblicata il
# 27/08/2026, licenza MIT, repository anthropics/anthropic-sdk-python. Aggiunge
# una sola dipendenza nuova (docstring-parser): le altre quattordici arrivano
# gia' con defeatbeta-api.
anthropic==1.2.0

# Il server dell'uso reale. Flask da solo ha un server di sviluppo che lo dice
# esso stesso di non usare in produzione: regge una richiesta alla volta e non
# ha nessuna difesa contro un client lento.
#
# gunicorn 23.0.0, licenza MIT, ~60 milioni di download al mese, progetto fermo
# ma maturo (e' l'ottava versione maggiore). ATTENZIONE: questa versione e'
# quella che conosco io — prima di installarla controlla se ce n'e' una piu'
# recente, e in quel caso pinna quella.
#
# Si lancia con UN WORKER SOLO e piu' thread, e il motivo sta in DEPLOY.md:
# `core/registry.py` tiene i lavori in memoria di processo, e con quattro worker
# il pulsante Stop cadrebbe tre volte su quattro sul processo sbagliato.
gunicorn==23.0.0
